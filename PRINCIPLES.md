# Agent workspace 节点归属文件系统原则

版本：2026-09-24，候选设计。本文描述新分支的目标，不声明实现或验收已完成。现有 KV 与旧文件系统能力继续按各自版本说明；新方向的机制见 [专题设计](docs/agent-home-preview-design.md)。

1. **一级目录选 home。** 中心只持久管理一级目录归属、节点注册及位置查询；调度器优先把 Agent 放到 home。目录以内的文件名、内容和属性由 home 的普通本机文件负责。
2. **本地是主路径。** home 上的 FUSE 将目录内操作透传本机文件系统，不逐操作向中心提交，也不强制分 Block。Native FS 是参照；薄 FUSE 开销须单独量化。
3. **远端可选 NFS 或 P2P。** 两个后端访问同一份 home 数据，挂载启动时选择；缺失远端路径必须明确失败。P2P 的 FUSE inode/handle 映射在请求节点，文件实际操作在 home。
4. **可见性以关闭后重开为基本边界。** 已关闭写者的新内容在另一节点后续打开时必须可见；`fsync` 成功须兑现本机保存承诺。旧已打开句柄、并发冲突、权限、失败写与节点失联各有显式错误和测试，不以任意缓存 TTL 代替保证。
5. **故障范围与数据归属一致。** home 进程退出后从本机目录和中心归属恢复；home VM 不可用时该目录不可用，恢复前不自动复制或接管。中心故障不能制造第二个 home。跨 home 迁移不在首版自动路径。
6. **共享成本移到共享路径。** 本地独享路径优先少做事；若启用缓存，第一次远端访问前必须围栏旧写者并处理数据/元数据失效。无法证明时禁用该缓存。异步预热有预算且不阻塞无关文件。
7. **按 Agent workload 验证价值。** 2–4 节点、两种远端后端和典型工作区操作必须有真实 Linux E2E。完整本地 W1 的两次独立六轮 p50 目标是默认 MooseFS 的至多 0.80；远端性能、薄 FUSE、Native FS 和耐久差异分别报告。失败也是结论，不能把理论收益写成已实现。
8. **可复现交付。** 同事应能在 Linux 从源码构建并生成安装包，再按文档部署和查询目录位置。首版不宣称完整通用 POSIX、长稳、HA 或正式发布。
