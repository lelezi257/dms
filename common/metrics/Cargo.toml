[package]
name = "afs-metrics"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Shared Prometheus registry and OpenMetrics exemplar support"
publish = false

[dependencies]
prometheus = { version = "0.14.0", default-features = false }
thiserror = "2.0.16"

[lints]
workspace = true
