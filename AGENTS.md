# DMS 开发协作规则

本分支从 `main` 开发 Agent workspace 节点归属文件系统预览版。先读本文件、[PRINCIPLES.md](PRINCIPLES.md) 和 [专题设计](docs/agent-home-preview-design.md)，再读当前模块代码与验收证据。原则描述目标，只有测试过的行为才可写成实现状态。现有 KV 与旧 Block 文件系统是独立历史产品路径，其细粒度提交、Block 生命周期与缓存规则不自动适用于新路径；修改旧模块时仍须保留其原有不变量。

## 产品边界

- 每台节点提供统一 FUSE 挂载；中心仅持久管理一级目录 home、节点注册和管理面位置查询。调度器查询位置后优先将 Agent 调度到 home。
- 一级目录内普通文件以 home 的本机文件树为权威。home 上操作直接透传本机文件；远端按启动配置走内核 NFS 或 P2P 文件操作转发，不能悄悄回退到另一个后端。
- 新本地主路径不能经过旧 `SharedFileOperations` 的逐写 Block/Meta 提交。共享、故障与迁移成本尽量放在对应慢路径，但成功回复、唯一 home、关闭后重开可见和 fsync 承诺不能削弱。
- 首版面向 2–4 节点 Agent workspace 常用文件/目录操作；完整通用 POSIX、自动 home 迁移/接管、Meta HA、长稳和正式发布不属于本轮完成声明。
- Native FS 只是性能参照。完整本地 W1 两份独立六轮 p50 均不高于默认 MooseFS 的 0.80 是性能目标；远端与共享分别报告。不能用不等语义的对照宣称强耐久优势。

## 代码与文档

- 新预览源码在 `server/homefs/`，构建/安装入口在 `scripts/homefs/`；旧 `server/`、SDK、protocol 保持既有合同，除非有具体兼容需求。不要让实验区原型直接充当产品代码或测试结果。
- 本文件只定开发约束；`PRINCIPLES.md` 是架构取舍唯一正文；`docs/agent-home-preview-design.md` 定具体机制与验收；`docs/agent-home-preview-installation.md` 面向同事安装。当前代码地图必须与实现同步，不把未来设计写成现状。
- 中心持久归属与 home 真实文件一致；根创建/删除有明确恢复状态。目录以内的数据不靠中心逐文件重建。注册信息可在节点重启后重报，归属不能丢。
- P2P 使用有界消息、明确错误、受信任私有网络与共享凭据；对路径穿越、符号链接逃逸和错误重试必须有测试。NFS 的内核缓存设置与 P2P 的可见性必须经相同业务用例验证。
- 若缓存失效/旧写围栏尚未证明，就采用保守缓存模式并记录性能代价；不能用 sleep、全机 drop_caches 或时间戳指纹冒充一致性协议。
- 大改先锁用户流程、故障边界和验收，新增抽象用一个真实 Agent case 解释。代码保持单一 owner，跨进程协议有版本/长度/错误边界，日志不打印文件内容或 token。

## 环境与验证

- macOS 宿主只作文件管理、编辑和阅读。Rust 构建、脚本测试、FUSE/NFS 运行、性能实验与构包均在约定 Linux VM/容器；保持 Rust toolchain 和 Cargo.lock。每个 Linux 构建使用私有 target 目录，不与 Mac 混用。
- 正确性先做定向回归，真实 FUSE E2E 检查实际 bytes、关闭后重开、失败码、进程/中心重启和 home VM 不可达；`fsync` 不能空返回成功。再做完整 W1 对照，冻结输入、二进制与环境，记录每轮和 p50/p95/p99。
- 安装包必须由源码在 Linux 构建，包含配置、启动和位置查询说明、许可材料与校验和；解包实际部署后才可声明可交同事测试。`package --list` 或单机编译不等于安装验收。
- 保留原工作区的未提交修改。本分支只从 `main` 独立开发；不要 push、merge、release 或启动旧 S6。当前用户已授权这次设计与实现的连续推进，普通文件修改和测试不需要反复请示；对真正改变产品承诺或不可逆操作才说明影响并请求决定。

## 交接

报告区分事实、推断、待验证和决策；附代码、命令、样本或安装包哈希。验收失败也必须给出确切阻断原因及可复现入口。每次阶段性收口更新受影响的工作区 `CURRENT.md`、`STATUS.md`、`NEXT.md`、`LOG.md`，重大取舍另记 `docs/decisions.md`；历史报告不因新方向被重写为通过。
