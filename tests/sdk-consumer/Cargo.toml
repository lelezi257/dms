[package]
name = "dms-sdk-consumer"
version = "0.0.0"
edition = "2024"
publish = false

# Keep this fixture independent from the parent workspace. It represents a
# normal external application whose manifest names only the public SDK crate.
[workspace]

[dependencies]
dms-client = { path = "../../sdk/rust/dms-client" }
